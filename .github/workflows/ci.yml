name: CI Demo

on:
    push:
          branches:
                  - main

          jobs:
              build:
                    runs-on: ubuntu-latest

                          steps:
                                  - name: 拉取代码
                                            uses: actions/checkout@v3

                                                    - name: 构建 Docker 镜像
                                                              run: |
                                                                          docker build -t my-first-devops .
